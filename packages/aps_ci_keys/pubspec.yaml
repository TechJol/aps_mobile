name: aps_ci_keys
description: A starting point for Dart libraries or applications.
version: 0.0.1

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  test: ^1.25.15
  very_good_analysis: ^8.0.0

